<p id="notice"><%= notice %></p>

<p>
  <strong>Brand:</strong>
  <%= @wallet.brand %>
</p>

<p>
  <strong>Driver License:</strong>
  <% if @wallet.driver_license.attached? %>
    <%= image_tag @wallet.driver_license.variant(resize: "500x500"), alt: @wallet.driver_license.filename %>
    <br />
    <%= link_to "Remove", wallet_driver_license_path(@wallet, @wallet.driver_license.id), method: :delete %>
  <% else %>
    Wallet has no driver license.
  <% end %>
</p>

<p>
  <strong>Credit Cards:</strong>
  <% if @wallet.credit_cards.attached? %>
    <% @wallet.credit_cards.each do |credit_card| %>
      <%= image_tag credit_card.variant(resize: "470x300>"), alt: credit_card.filename %>
      <br />
      <%= link_to "Remove", wallet_credit_card_path(@wallet, credit_card), method: :delete %>
    <% end %>
  <% else %>
    Wallet has no credit cards.
  <% end %>
</p>

<%= link_to 'Edit', edit_wallet_path(@wallet) %> |
<%= link_to 'Back', wallets_path %>
